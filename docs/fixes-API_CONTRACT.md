# Fixes & New Endpoints — API Contract

This file documents **new or modified API endpoints** introduced as part of the Student Portal & Recruiter Portal update. The backend team should implement these to replace the current mock implementations.

---

## Token Format (Important)

Share tokens are **self-contained** — the student IDs are encoded directly into the token using base64url encoding. This means:

- Share links work in any browser, any device, and any deployment **without needing shared server state to validate the token**.
- The backend should decode the token to extract student IDs rather than look them up in a database table (though storing them for audit/revocation purposes is fine).

**Encoding (frontend generates, backend validates):**
```
token = base64url( JSON.stringify(studentIds[]) )
```
Example: `["student-1", "student-2"]` → `WyJzdHVkZW50LTEiLCJzdHVkZW50LTIiXQ`

**Special token:** `demo1234` is a reserved demo token that always resolves to all students (dev/testing only).

> [!NOTE]
> The frontend generates the share URL using `window.location.origin + "/share/" + token` — no hardcoded domain. This works correctly on any deployment (local, Vercel preview, production) automatically.

---

## Deployment Note

The frontend is a **Single Page Application (SPA)**. All routing is handled client-side by React Router. To prevent 404 errors when recruiters open share links directly, the hosting provider must rewrite all unmatched routes to `index.html`.

**Vercel** (`vercel.json`):
```json
{
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
```

**Nginx:**
```nginx
location / {
  try_files $uri $uri/ /index.html;
}
```

---

## 1. Feature / Unfeature a Project

### `PATCH /me/projects/:id/feature`

Toggles the `is_featured` flag on a project. If the student already has **3 featured projects**, the backend must reject the request.

**Auth:** Required (student JWT)

**Path Parameters:**
| Param | Type   | Description |
|-------|--------|-------------|
| `id`  | string | Project UUID |

**Request Body:** _(empty)_

**Success Response — `200 OK`:**
```json
{
  "isFeatured": true
}
```

**Error Response — `422 Unprocessable Entity`** (when 3 projects are already featured):
```json
{
  "detail": "You can feature at most 3 projects on your recruiter profile."
}
```

---

## 2. Recruiter Portal — List Students in Bundle

### `GET /share/:token`

Returns the student listing for a shared bundle link.  
**This endpoint requires NO authentication.** It is publicly accessible.

**Path Parameters:**
| Param   | Type   | Description |
|---------|--------|-------------|
| `token` | string | Share token generated by a teacher |

**Success Response — `200 OK`:**
```json
{
  "token": "abc12345",
  "createdAt": "2026-07-01T00:00:00Z",
  "expiresAt":  "2026-07-31T00:00:00Z",
  "studentCount": 10,
  "students": [
    {
      "id": "uuid",
      "name": "Aarav Sharma",
      "department": "Computer Engineering",
      "batch": "2022-2026",
      "avatar": null,
      "cgpa": 9.5,
      "topSkills": ["React", "Python", "TensorFlow"],
      "featuredProjectCount": 2,
      "hasExperience": true
    }
  ]
}
```

> [!NOTE]
> `topSkills` returns a maximum of **3** skills (by proficiency descending).  
> `featuredProjectCount` counts only projects where `is_featured = true`.

**Error Response — `404 Not Found`** (expired or invalid token):
```json
{ "detail": "Share link not found or has expired." }
```

---

## 3. Recruiter Portal — Single Student Profile

### `GET /share/:token/:studentId`

Returns the **recruiter-safe, redacted profile** for a single student.  
**This endpoint requires NO authentication.** It is publicly accessible.

**Path Parameters:**
| Param       | Type   | Description |
|-------------|--------|-------------|
| `token`     | string | Share token |
| `studentId` | string | Student UUID |

**Success Response — `200 OK`:**
```json
{
  "id": "uuid",
  "name": "Aarav Sharma",
  "department": "Computer Engineering",
  "batch": "2022-2026",
  "avatar": null,
  "cgpa": 9.5,
  "bio": "Passionate Computer Engineering student...",
  "domainInterest": "Web Development",
  "github": "https://github.com/aarav-sharma",
  "portfolio": null,
  "linkedin": null,
  "resumePdf": null,
  "projects": [
    {
      "id": "proj-uuid",
      "name": "Smart Campus Navigation System",
      "description": "...",
      "domain": "College Project",
      "techStack": ["React Native", "Node.js"],
      "status": "Completed",
      "type": "College Project",
      "githubRepo": "https://github.com/...",
      "liveUrl": null,
      "startDate": "2025-08-10T00:00:00Z",
      "endDate": "2025-11-20T00:00:00Z",
      "isFeatured": true
    }
  ],
  "skills": {
    "technical": [
      { "name": "React", "domain": "Web Development", "proficiency": 5 }
    ],
    "soft": [
      { "name": "Leadership", "proficiency": 5 }
    ],
    "languages": [
      { "name": "English", "proficiency": "Fluent" }
    ]
  },
  "experience": [
    {
      "id": "exp-uuid",
      "organisation": "TechFlow Innovations",
      "role": "Software Engineering Intern",
      "type": "Internship",
      "description": "...",
      "startDate": "2025-05-15T00:00:00Z",
      "endDate": "2025-07-15T00:00:00Z"
    }
  ],
  "achievements": [
    {
      "id": "ach-uuid",
      "title": "Smart India Hackathon Finalist",
      "description": "...",
      "category": "Technical",
      "type": "Hackathon",
      "level": "National",
      "date": "2025-12-10T00:00:00Z",
      "certificateUrl": "https://..."
    }
  ]
}
```

**Project logic:**
- If the student has **any** featured projects (`is_featured = true`), return **only** those.
- If the student has **no** featured projects, return the **3 most recently created** projects.

**Error Response — `404 Not Found`:**
```json
{ "detail": "Student not found in this share bundle." }
```

---

## 4. Create a Share Bundle (Teacher)

### `POST /share`

Creates a new share bundle and returns a token.

**Auth:** Required (teacher JWT)

**Request Body:**
```json
{
  "studentIds": ["uuid1", "uuid2", "uuid3"],
  "expiresInDays": 30
}
```

**Success Response — `201 Created`:**
```json
{
  "token": "WyJzdHVkZW50LTEiLCJzdHVkZW50LTIiXQ",
  "shareUrl": "https://<your-domain>/share/WyJzdHVkZW50LTEiLCJzdHVkZW50LTIiXQ",
  "createdAt": "2026-07-06T00:00:00Z",
  "expiresAt":  "2026-08-05T00:00:00Z",
  "studentCount": 3
}
```

> [!NOTE]
> The `shareUrl` should be constructed from the request's `Origin` header or a configured base URL — never hardcoded. The frontend always uses `window.location.origin` so the URL is correct on any deployment.

---

## 5. Revoke a Share Bundle (Teacher)

### `DELETE /share/:token`

Permanently revokes a share bundle, making the link inaccessible immediately.

**Auth:** Required (teacher JWT)

**Success Response — `204 No Content`**

**Error Response — `404 Not Found`** if the token does not belong to the requesting teacher.

---

## 6. List Teacher's Share Bundles

### `GET /share`

Returns all active (non-expired) share bundles created by the authenticated teacher.

**Auth:** Required (teacher JWT)

**Success Response — `200 OK`:**
```json
[
  {
    "token": "abc12345",
    "shareUrl": "https://portfolio.mitwpu.edu.in/share/abc12345",
    "createdAt": "2026-07-01T00:00:00Z",
    "expiresAt":  "2026-07-31T00:00:00Z",
    "studentCount": 5,
    "isExpired": false
  }
]
```
